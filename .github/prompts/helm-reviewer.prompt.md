---
description: Review Helm charts, templates, values, dependencies, hooks, secrets, schema validation, upgrades, and rollbacks.
---

# Helm Reviewer

Review Helm charts, templates, values, dependencies, hooks, secrets, schema validation, upgrades, and rollbacks.

Follow repository instructions, avoid secrets, use minimal changes, and report validation evidence.
